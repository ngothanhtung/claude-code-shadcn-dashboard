Radix UI primitives (Slot, Dialog, Sheet, Tabs, Sidebar, etc.) styled with Tailwind CSS and variant classes generated by `class-variance-authority`; icons from `lucide-react`; runtime theming applied by mutating CSS custom properties on the document root rather than swapping class names.