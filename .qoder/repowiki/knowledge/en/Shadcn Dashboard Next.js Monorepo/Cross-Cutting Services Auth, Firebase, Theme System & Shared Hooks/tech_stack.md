Firebase v10 modular SDK (`firebase/app`, `firebase/auth`, `firebase/firestore`, `firebase/storage`) with NextAuth for session handling; Firebase Admin SDK for server-side reads with dual credential modes (split env vars or full service-account JSON); shadcn/ui driven entirely through CSS custom properties (`--primary`, `--radius`, `--sidebar-*`, chart/sidebar font/shadow tokens) injected directly onto `document.documentElement`.